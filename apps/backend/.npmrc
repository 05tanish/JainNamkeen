# NPM Configuration for Docker builds
# This helps resolve peer dependency conflicts

# Use legacy peer dependency resolution
legacy-peer-deps=true

# Disable strict SSL for corporate networks (remove if not needed)
# strict-ssl=false

# Use longer timeout for slow networks
fetch-timeout=60000

# Disable progress bar in CI/Docker
progress=false

# Use flat node_modules structure
legacy-bundling=false
